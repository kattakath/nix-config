# Fleet manifest for the fleet-doctor skill.
#
# One repo per line, relative to ~/Developer, forge/owner/repo — the SAME
# path shape every repo already lives under (see root CLAUDE.md, global
# ~/.claude/CLAUDE.md "repos live under ~/Developer/<forge>/<owner>/<repo>").
#
# This is nix-config's own composition (this repo) + the private composition
# flake + the standalone fleet repos nix-config or its private layer relates to.
# It is NOT "every repo on disk" — client/personal repos with no
# Nix/fleet relationship (Infin8, dontsell-ai, silvercreek-ai, deepjobseek,
# etc.) are deliberately excluded; fleet-doctor is scoped to declarative-infra
# hygiene, not general dev-repo housekeeping.
#
# Membership does NOT require nix-config to consume the flake as an input:
# ircc-whatsapp-bot is listed and is not a nix-config input at all.
# The test is "is this a fleet repo we maintain", not "is it adopted".
#
# NOR does membership require a flake.nix — the test says "fleet repo", not
# "fleet flake", and three listed members deliberately have none. A sweep must
# not try to `nix flake check` these, nor conclude they don't belong here:
#   brags                 — data + engine behind the `brag` skill
#   provisioner-template  — the scaffold `vast-init-repo` clones; a live Vast
#                           instance fetches its provision.sh over raw HTTP at boot
#   hyperframes-selfhost  — the published self-host bundle (this repo vendors a
#                           copy under packages/hyperframes-selfhost/)
#
# Deliberately NOT listed: character-mcp (extracted from nix-personal 2026-09-06)
# is a deployed Node/Cloudflare-Worker product with no Nix surface at all —
# personal, not declarative infra, so it fails the scoping rule above.
#
# Maintenance: when a new fleet repo appears (extracted or greenfield), add one
# line here. Nothing else needs to change — the skill iterates this file.
#
# ADR-002 ran this list the OTHER way, and it is DONE: every satellite absorbed into
# nix-config as a capsule is archived and its line came OUT, because a doctor that sweeps
# an archived repo reports stale branches nobody can merge. All seven left, in wave order:
#   wave 3  nix-cloudflared-connector  -> modules/features/cloudflared-connector/
#   wave 4  nix-firmware-secrets       -> modules/features/firmware-secrets/
#   wave 4  nix-keychain-secrets       -> modules/features/keychain-secrets/
#   wave 4  nix-vast-provision         -> modules/features/vast-provision/
#   wave 5  nix-tart-vms               -> modules/features/tart-vms/
#   wave 5  nix-media-cli              -> modules/features/media-cli/
#   wave 6  nix-local-rag              -> modules/features/local-rag/
# Do not re-add one because it is still on disk — the working copy outlives the repo.
# The satellite count is 0; what is left below is the composition pair plus the repos
# that were never satellites.
#
# TWO MORE LINES CAME OUT on 2026-09-12, for a DIFFERENT reason. These were never
# satellites and never became capsules: they were extraction candidates nix-config never
# adopted, so nothing arrived in-tree when they left. Archived, not deleted (ADR-002 §7.8)
# — both stay public and readable, which is where the "why it wasn't adopted" lives:
#   nix-mcp-gateway  — a THIN, GENERIC `services.mcpGateway` broker module. The fleet kept
#                      its own fully-wired ~20-server deployment in modules/shared/mcp.nix,
#                      which carries the long-form note (one generic module vs. one
#                      concrete deployment — never two competing options).
#   nix-inngest      — never wired into any host. The `inngest` CLI that IS on PATH comes
#                      from nixpkgs, not from this repo; do not confuse the two.
# Same rule as the seven above: do not re-add either because a working copy is on disk.

github.com/kattakath/nix-config
gitlab.com/ismailkattakath/nix-personal
github.com/kattakath/brags
github.com/kattakath/ircc-whatsapp-bot
github.com/kattakath/provisioner-template
github.com/ismailkattakath/hyperframes-selfhost
