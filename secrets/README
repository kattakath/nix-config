Encrypt the GitHub token secret (run once from the repo root, requires the private key for the public key in secrets.nix):

    agenix -e secrets/github-token.age

The resulting secrets/github-token.age must be committed to the repo.
agenix decrypts it at home-manager activation time using ~/.ssh/id_ed25519.
