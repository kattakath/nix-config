# Architecture diagrams

- **Rendered SVG (GitHub-native):** [architecture.svg](./architecture.svg) — hand-authored, exact labels.
- **README Mermaid:** live-editable graph for PRs.

Prefer editing the SVG or Mermaid when labels must stay exact. Free-form Excalidraw boards can be dropped here as `.excalidraw` exports if you sketch offline; keep the SVG as the source of truth for README embeds so CI/docs never depend on binary blobs.
