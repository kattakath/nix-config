# page-lab — measured facts

**A row marked UNVERIFIED is not promoted by editing this table — it is promoted by re-running its
recipe and dating the result.** Editing the date, the wording, or the `Measured` column without
re-running the recipe is a fabrication, not an update. Same discipline as `patterns.md`'s
*Flagged — NOT verified* table and `probes.md`'s *Honest caveats*: epistemic hygiene, **not a backlog**.

## What this file is

Every fact in this plugin that an upstream bump can falsify lives here **once**, with an ID, a date
and a recipe. Everywhere else states the fact in at most one clause and cites `[F-ID]`.

- Citing: write `[F-CDP-29TOOLS]` inline. `checks.<system>.page-lab` fails the build on a `[F-…]`
  citation with no row here.
- Restating: do not. A second prose copy of a volatile fact is the drift this file exists to kill.
- Promoting an UNVERIFIED row: run its recipe, paste the observed result into the `Fact` cell,
  move the row into the measured table, and date it **the day you ran it**.
- `Cited by` is maintained by hand and is a convenience, not an authority — the CI grep is.
- Recipes are written for the **rendered** table: a `\|` in the raw source is a literal `|` in the
  command. Copy from the rendered view, or unescape — an escaped pipe pasted into a shell is a
  literal character, not a pipeline.

## Two harnesses the recipes reuse

Run both under Node 22 (`nix shell nixpkgs#nodejs_22 -c bash`) — the fleet's default Node has no
global `WebSocket` [F-NODE-NO-WS]. Paths are relative to `plugins/page-lab/`.

```bash
# H-MCP — dump a stdio MCP server's tool names.  Usage: h_mcp npx -y <pkg>@<ver> [flags...]
h_mcp() {
  printf '%s\n' \
    '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2024-11-05","capabilities":{},"clientInfo":{"name":"page-lab-facts","version":"0"}}}' \
    '{"jsonrpc":"2.0","method":"notifications/initialized"}' \
    '{"jsonrpc":"2.0","id":2,"method":"tools/list"}' \
    | "$@" 2>/dev/null | jq -c 'select(.id==2) | .result.tools | map(.name)'
}

# H-CDP — send ONE CDP method to the first page target of a debug browser on :9222.
# Usage: h_cdp Overlay.setInspectMode '{"mode":"none"}'
h_cdp() {
  params="${2:-}"; [ -n "$params" ] || params='{}'
  node --input-type=module -e '
    import { browserSocket, connect, pageTargets, attach } from "./scripts/lib/cdp.mjs";
    const c = await connect(await browserSocket("http://127.0.0.1:9222"));
    const t = (await pageTargets(c))[0];
    const s = await attach(c, t.targetId);
    await c.send("DOM.enable", {}, s);
    await c.send("DOM.getDocument", { depth: 0 }, s);
    console.log(JSON.stringify(await c.send(process.argv[1], JSON.parse(process.argv[2]), s)));
    c.close();
  ' "$1" "$params"
}
```

## Measured facts

| ID | Fact | Measured | How to re-measure | Cited by |
|---|---|---|---|---|
| `F-CDP-29TOOLS` | `chrome-devtools-mcp@1.8.0` exposes exactly **29** tools, identical in attach and launch mode. Upstream's generated `docs/tool-reference.md` describes **~57** from `main`; `--slim` is 3. Absent from the release: the **entire Extensions group** (`install_extension`, `list_extensions`, `reload_extension`, `trigger_extension_action`, `uninstall_extension`), 12 of 13 Memory tools, all PWA, all 3P/WebMCP, `click_at`, `screencast_start/stop`. **Any plan depending on `install_extension` is invalid today** — the clean-room "load a manager into an isolated instance" route does not exist. | 2026-09-06 | `bash scripts/devtools-doctor.sh --verify-tools` — prints `TOOLS_COUNT=` and `TOOLS_DRIFT=`. By hand: `h_mcp npx -y chrome-devtools-mcp@1.8.0 --no-usage-statistics --no-performance-crux` and count the names. | `skills/page-diagnose/SKILL.md`, `skills/page-diagnose/references/tools.md`, `references/pick-protocol.md`, `scripts/devtools-doctor.sh` |
| `F-NO-CDP-PASSTHROUGH` | Nothing in `chrome-devtools-mcp` at any version sends an arbitrary CDP command. A raw-WS side channel is structural, not a stopgap. | 2026-09-06 | `h_mcp npx -y chrome-devtools-mcp@1.8.0 --no-usage-statistics --no-performance-crux` then scan the names for anything protocol-shaped: `grep -i -e cdp -e protocol -e send_command -e raw` — must be empty. | `references/cdp-extras.md`, `references/pick-protocol.md`, `references/routes.md` |
| `F-DOLLAR-ZERO-DEAD` | `$0` is **not** readable from a separate CDP session — `undefined` even with `includeCommandLineAPI:true`. It is bound per-session via `DOM.setInspectedNode`. Do not retry this. | 2026-09-06 | With DevTools open on the tab and a node selected: `h_cdp Runtime.evaluate '{"expression":"$0","includeCommandLineAPI":true}'` → `result.type == "undefined"`. | `references/pick-protocol.md`, `references/cdp-extras.md` |
| `F-INSPECTMODE-WORKS` | `Overlay.setInspectMode({mode:'searchForNode'})` works from a non-DevTools CDP client and `Overlay.inspectNodeRequested` fires on the user's click with a `backendNodeId`. No DevTools window needed. | 2026-09-07 | `page-lab-pick --timeout 20000`, then click any element in the debug browser: exit 0 and exactly one `page-lab/pick@1` object on stdout. | `README.md`, `references/pick-protocol.md`, `references/routes.md`, `scripts/pick-element.mjs` |
| `F-DISARM-HIGHLIGHTCONFIG` | `Overlay.setInspectMode({mode:'none'})` **rejects** with `Internal error: highlight configuration parameter is missing` unless `highlightConfig` is passed on the disarm too. A single `try/catch` around the disarm loop therefore leaves **every** armed tab armed. This is the mechanical root cause of the swallowed-click failure. | 2026-09-07 | `h_cdp Overlay.enable` then `h_cdp Overlay.setInspectMode '{"mode":"none"}'` → the quoted error; repeat with `'{"mode":"none","highlightConfig":{"contentColor":{"r":0,"g":0,"b":0,"a":0}}}'` → success. | `references/pick-protocol.md`, `scripts/lib/cdp.mjs`, `scripts/pick-element.mjs`, `scripts/pick-watchdog.mjs` |
| `F-ARMED-SWALLOWS` | An armed picker swallows the next click on **every** armed tab. | 2026-09-06 | Arm with `page-lab-pick`, then click a link in a tab you did not intend to pick from: the link does not navigate and the click is consumed by inspect mode. | `references/pick-protocol.md`, `references/routes.md`, `commands/pick.md` |
| `F-MULTI-CLIENT` | Chrome has supported multiple simultaneous CDP clients per target since Chrome 63, no configuration. An open DevTools window does not block a second client. | 2026-09-06 | Open DevTools on a tab, then run `page-lab-pick` against the same browser — both stay live. | `skills/page-diagnose/references/attaching.md`, `references/routes.md` |
| `F-TARGETS-13-3` | `/json/list` returned 13 targets, of which only **3** were `type === "page"`. Arm page targets only; arming one tab fails in practice because the operator clicks whichever tab they are looking at. | 2026-09-06 | `curl -s http://127.0.0.1:9222/json/list \| jq '[length, ([.[] \| select(.type=="page")] \| length)]'`. **Recipe needs classic launch-flag mode**: in `chrome://inspect` consent mode every `/json/*` path is 404 and this cannot run as written [F-NO-JSON-HTTP]; the fact itself is NOT re-measured. | `scripts/pick-element.mjs`, `references/pick-protocol.md`, `references/routes.md` |
| `F-NODEID-EPHEMERAL` | `DOM.documentUpdated` invalidates **all** nodeIds and fires on navigation and SPA document replacement. Always re-resolve from `backendNodeId`. | 2026-09-06 | Resolve a nodeId, navigate the tab, then reuse it: `h_cdp DOM.describeNode '{"nodeId":<old>}'` → `Could not find node with given id`. | `scripts/pick-element.mjs`, `references/pick-protocol.md` |
| `F-STICKY-STATE` | Overlay and Emulation state persists across navigation and is **shared with every CDP client including the operator's own DevTools**. Pair every override with its clear. | 2026-09-06 | `h_cdp Emulation.setDeviceMetricsOverride '{"width":390,"height":844,"deviceScaleFactor":3,"mobile":true}'`, let the socket close, then look at the tab and at DevTools' own device bar — still overridden. | `references/cdp-extras.md`, `references/routes.md`, `scripts/lib/cdp.mjs` |
| `F-MCP-SURVIVES-CLOSED-PORT` | `chrome-devtools-mcp` answers `initialize` and stays alive with nothing listening on the port; browser contact is lazy. It does **not** dark the gateway. | 2026-09-06 | Quit every Chromium, confirm `curl -s --max-time 2 http://127.0.0.1:9222/json/version` fails, then run `h_mcp npx -y chrome-devtools-mcp@1.8.0 --browser-url http://127.0.0.1:9222 --no-usage-statistics --no-performance-crux` → the tool list still returns. **Recipe needs classic launch-flag mode**: in `chrome://inspect` consent mode every `/json/*` path is 404 and this cannot run as written [F-NO-JSON-HTTP]; the fact itself is NOT re-measured. | `skills/page-diagnose/references/attaching.md`, `scripts/devtools-doctor.sh` |
| `F-NO-JSON-HTTP` | A browser put into debugging mode from `chrome://inspect/#remote-debugging` serves the CDP **WebSocket** but **not** the `/json/*` HTTP discovery surface — `/`, `/json`, `/json/list`, `/json/version`, `/json/protocol` and `/devtools/inspector.html` all return **HTTP 404**, while `Browser.getVersion` over the WS endpoint from `DevToolsActivePort` succeeds. Measured on **both** Chromium 152 (:9222) and Opera Air OPR/135 (:61867), neither of which had `--remote-debugging-port` in its argv. **This kills `--browser-url` outright**, since puppeteer's `connect({browserURL})` must GET `/json/version` to find the WS URL — the live gateway failed with `Failed to fetch browser webSocket URL from http://127.0.0.1:9222/json/version: HTTP Not Found`. The complement IS now measured and is the opposite: a browser launched the classic way with `--remote-debugging-port` serves `/json/version` and `/json/list` with **HTTP 200** (Opera Air, same build, relaunched with `--remote-debugging-port=61867`). So the 404 is a property of **consent mode**, not of the browser or the version — and `--browser-url` is correct for a launch-flag browser and only for one. | 2026-09-07 | `for p in / /json /json/list /json/version; do curl -s -o /dev/null -w "%{http_code} $p\n" --max-time 4 "http://127.0.0.1:<port>$p"; done` → all 404, where `<port>` is line 1 of `<user-data-dir>/DevToolsActivePort`. Then confirm CDP itself is alive on the WS path in line 2 of that same file. | `modules/shared/mcp.nix`, `skills/page-diagnose/references/attaching.md`, `scripts/devtools-doctor.sh` |
| `F-AUTOCONNECT-USERDATADIR` | `--autoConnect` resolves the endpoint by reading `DevToolsActivePort` from the profile named by **`--userDataDir`**, so it works against any browser and needs no port or UUID configured anywhere. Verified by its own failure path: bare `--autoConnect` errors with `Could not find DevToolsActivePort for chrome at ~/Library/Application Support/Google/Chrome/DevToolsActivePort`, and adding `--userDataDir` redirects that lookup. This is what makes a declarative attach possible in **consent mode**, where the port and the browser WebSocket UUID both change every launch. It is **NOT** a universal answer: against a launch-flag browser the file goes stale and `--autoConnect` attaches to a dead socket [F-DEVTOOLSACTIVEPORT-STALE], so it must be chosen by probing, not assumed. `--wsEndpoint ws://…/devtools/browser/<uuid>` also works but must be re-typed per launch. | 2026-09-07 | `npx -y chrome-devtools-mcp@latest --autoConnect --userDataDir "$HOME/Library/Application Support/com.operasoftware.OperaAir" --no-usage-statistics --no-performance-crux`, then call the page-listing tool → real tabs. Drop `--userDataDir` → the quoted Google/Chrome error. | `modules/shared/mcp.nix`, `skills/page-diagnose/references/attaching.md` |
| `F-DEVTOOLSACTIVEPORT-STALE` | `DevToolsActivePort` line 2 (the browser WebSocket path) is a cached copy the browser does **not** reliably refresh, and a stale line 2 is **dead**, not merely old. Measured on Opera Air relaunched with `--remote-debugging-port=61867`: the file's mtime was `07:08:27` while the browser started `09:09:32` — **never rewritten**. Its `3033135b-…` UUID failed the WebSocket upgrade (`non-101 status code`) while `/json/version` served a live `d58fe459-…`. Consequence: **`--autoConnect` fails against a launch-flag browser** (`Could not connect to Chrome … Cause: Unexpected server response: 404`) even though the browser is up and `--browser-url` attaches fine. Line **1** (the port) stayed correct throughout — it is the trustworthy half. | 2026-09-07 | Relaunch a browser with `--remote-debugging-port=<same port>`, then compare `sed -n 2p <user-data-dir>/DevToolsActivePort` against the path in `curl -s http://127.0.0.1:<port>/json/version \| jq -r .webSocketDebuggerUrl`. Different ⇒ stale; open a WS to each and only the live one upgrades. `bash scripts/devtools-doctor.sh` reports it as `WARN … is STALE`. | `modules/shared/mcp.nix`, `scripts/lib/cdp.mjs`, `scripts/devtools-doctor.sh`, `skills/page-diagnose/references/attaching.md` |
| `F-UGC-NO-VENDOR` | ungoogled-chromium reports `"Browser":"Chrome/152.0.7977.64"` on `/json/version` with **no vendor field**. The build string cannot identify the browser — only the running process path can. | 2026-09-06 | `curl -s http://127.0.0.1:9222/json/version` (no vendor key) versus `pgrep -fl -- '--remote-debugging-port'` (real bundle path). **Recipe needs classic launch-flag mode**: in `chrome://inspect` consent mode every `/json/*` path is 404 and this cannot run as written [F-NO-JSON-HTTP]; the fact itself is NOT re-measured. In consent mode `pgrep` also finds nothing — identify the serving app with `lsof -nP -iTCP:<port> -sTCP:LISTEN` instead. | `scripts/page-route.sh`, `scripts/devtools-doctor.sh`, `skills/page-diagnose/references/attaching.md` |
| `F-UGC-ATTACHES` | `chrome-devtools-mcp` attaches to unsupported Chromium builds despite upstream supporting only Google Chrome / Chrome for Testing. Re-confirmed 2026-09-07 against **Opera Air** (`OPR/135`, Chromium 151): 29 tools and `list_pages` returned its real tabs. The `--browser-url` route in the original recipe is dead [F-NO-JSON-HTTP]; use `--autoConnect` or `--wsEndpoint`. | 2026-09-07 | Point `--autoConnect --userDataDir <profile>` at the unsupported build's user-data dir and call its page-listing tool (name in `skills/page-diagnose/references/tools.md`) — it returns the open pages. | `skills/page-diagnose/references/attaching.md`, `README.md` |
| `F-CASK-NOFLAG` | The fleet browser is a Homebrew cask ⇒ `programs.chromium.package = null` ⇒ upstream's assertion **forbids** `commandLineArgs`. `--remote-debugging-port` **can never be declarative here**; it is a hand-run wrapper. | 2026-09-06 | `src=$(nix eval --raw --impure --expr 'builtins.toString (builtins.getFlake "'"$PWD"'").inputs.home-manager.outPath'); grep -n 'commandLineArgs' "$src/modules/programs/chromium.nix"` → the assertion; and `nix eval .#darwinConfigurations.macos.config.home-manager.users.ismail.programs.chromium.package` → `null`. | `references/routes.md`, `README.md`, `scripts/route-up.sh` |
| `F-KAPTURE-COLD` | Measured default: the Kapture bridge is up on `127.0.0.1:61822` and `/tabs` returns `[]` — zero tabs connected. A running bridge with zero tabs is **dark**, not ready. | 2026-09-06 | `curl -s --max-time 2 http://127.0.0.1:61822/tabs` — `[]` is bridge-up-zero-tabs; connection refused is bridge-down. Two different problems. | `scripts/page-route.sh`, `references/routes.md`, `scripts/route-up.sh` |
| `F-KAPTURE-EVAL-GATE` | `kapture-mcp` **filters `evaluate` out of `tools/list`** until a connected tab has *Allow JavaScript Execution* on (`dist/tool-handler.js:16`), and fires `notifications/tools/list_changed` when it flips. The toggle is **in-memory and resets on disconnect** (`modules/tab-state.js:56`). Never assume `mcp__kapture__evaluate` exists; probe by **name presence**, never by calling it. | 2026-09-06 | Source: `f=$(find "$(npm config get cache)/_npx" -path '*kapture-mcp*' -name tool-handler.js); sed -n '1,30p' "$f"`. Behaviour: check whether `mcp__kapture__evaluate` is in the session tool list with the toggle off, flip it in the popup, check again. | `references/routes.md`, `references/pick-protocol.md`, `scripts/route-up.sh` |
| `F-KAPTURE-POPUP` | Extension 1.2.1 exposes the connect toggle in the **toolbar popup** (`popup.js:31-33`); DevTools is not required. `modules/shared/chromium.nix`'s option description saying otherwise is stale and is fixed by this PR. | 2026-09-06 | Close DevTools entirely, click the Kapture toolbar icon, connect the tab, then `curl -s http://127.0.0.1:61822/tabs` → one entry. Source: grep `popup.js` in the pinned 1.2.1 extension directory for the connect handler. | `references/routes.md`, `README.md`, `modules/shared/chromium.nix` |
| `F-KAPTURE-MUTATES` | `getUniqueSelector()` stamps `id="kapture-N"` or adds class `.kapture-N` on any element Kapture returns. The selector is real, unique, and **evaporates on reload**. Fine to act on; **never** allowed into a `.user.js`. It also dirties the DOM, contaminating any later HTML diff. | 2026-09-06 | Connect a tab, `mcp__kapture__elements({tabId, selector:"h1"})`, then look at the returned selector and at the live DOM: an injected `kapture-N` id/class. Reload — it is gone. | `references/pick-protocol.md`, `references/routes.md`, `scripts/pick-normalize.mjs`, `scripts/userscript-meta-lint.sh` |
| `F-KAPTURE-SKEW` | Server half is `npx -y kapture-mcp@latest bridge` in `~/.claude.json` (imperative, unpinned); extension half is pinned at **1.2.1** in Nix. The bridge already version-gates the extension (`EXTENSION_OUTDATED`, network tools need ≥1.1.0), so skew is a precedented failure. | 2026-09-06 | `jq '.mcpServers.kapture' ~/.claude.json` (unpinned `@latest`) versus `grep -n 'kapture' modules/shared/chromium.nix` (pinned 1.2.1). | `references/routes.md`, `README.md` |
| `F-INAPP-COLD` | `mcp__Claude_Browser__javascript_tool` refuses with *"No preview is open"* until a pane exists; `mcp__Claude_Browser__navigate` with a `url` opens one and the same call then succeeds. **Not probeable from a shell** — it is an in-session MCP tool, like the kapture and cic gates. | 2026-09-07 | Call `mcp__Claude_Browser__javascript_tool` before any `navigate` in a fresh session; then `navigate` to any URL and repeat. There is deliberately no shell command; that absence is the fact. | `references/routes.md`, `scripts/page-route.sh` |
| `F-INAPP-ZERO-VIEWPORT` | A collapsed or hidden in-app pane still executes JS, but the page reports `innerWidth`/`innerHeight` **0** and lays out nothing — a masonry read there returned `columns: 0` with every rect null, which reads exactly like a broken selector. `resize_window` to an explicit size, then reload, before measuring geometry: the same page then returned 4 columns and 8px gaps. | 2026-09-07 | In the in-app browser on any responsive page, evaluate `[innerWidth, innerHeight]` with the pane collapsed, then `mcp__Claude_Browser__resize_window {width:1512,height:900}`, reload, and evaluate again. | `references/routes.md`, `scripts/page-route.sh` |
| `F-CIC-COLD` | `mcp__claude-in-chrome__tabs_context_mcp` answers *"Browser extension is not connected"* by default. **Not probeable from a shell** — its transport is native messaging. | 2026-09-06 | Call `mcp__claude-in-chrome__tabs_context_mcp` in a session with the extension closed. There is deliberately no shell command here; that absence is the fact. | `references/routes.md`, `scripts/page-route.sh`, `scripts/route-up.sh` |
| `F-CIC-REPLMODE` | claude-in-chrome wraps source in a `{ … }` block with `replMode:true`. Top-level `let`/`const` are gone by the next call; all state must hang off `window`. Single evaluation capped ~40 s, output 50 KB. | 2026-09-06 | Two `mcp__claude-in-chrome__javascript_tool` calls: `let probe = 1` then `probe` → ReferenceError; `window.probe = 1` then `window.probe` → `1`. | `scripts/pick-payload.js`, `references/pick-protocol.md` |
| `F-CIC-SANITIZER` | Output is **silently** sanitized, no error, value replaced. String with `=` and (`;` or `&`) → `[BLOCKED: Cookie/query string data]`; `^[A-Za-z0-9_-]+\.[A-Za-z0-9_-]+\.[A-Za-z0-9_-]+$` → `[BLOCKED: JWT token]` (**a class chain like `div.card.active` matches exactly**); `^[A-Za-z0-9+/]{20,}={0,2}$` → base64; `^[a-f0-9]{32,}$` → hex credential; key matching `/password\|token\|secret\|api[_-]?key\|auth\|credential\|private[_-]?key\|access[_-]?key\|bearer\|oauth\|session/i` → sensitive key (**a key named `author` trips `/auth/i`**); `cookie`/`cookies` keys blocked; strings >1000 chars truncated; arrays truncated to 100; a top-level array returns only its description; a DOM node returns `[DOM Node]`. | 2026-09-06 | Run the canary object through `mcp__claude-in-chrome__javascript_tool` and compare against the documented substitutions: `({a:'div.card.active', b:'x=1;y=2', c:'<<probe>>', d:'a'.repeat(40)})`. This is the same canary the tier-4 ARM call ships. | `references/pick-protocol.md`, `scripts/pick-payload.js`, `scripts/pick-normalize.mjs` |
| `F-CIC-ORIGIN-RECHECK` | claude-in-chrome re-checks the origin **after** execution: a same-call navigation returns *"Security check failed: the page navigated to a different domain"*. Capture-phase `preventDefault` is mandatory. | 2026-09-06 | On a permitted domain, one `mcp__claude-in-chrome__javascript_tool` call whose body does `location.href='https://example.com'` → the quoted error instead of a result. | `scripts/pick-payload.js`, `references/pick-protocol.md` |
| `F-NODE-NO-WS` | Node v20.20.2 on this fleet has **no global `WebSocket`**; `--experimental-websocket` supplies one; Node 22+ has it unflagged. `packages/page-lab-pick.nix` pins `nodejs_22` so no flag is used in the fleet path. | 2026-09-06 | `node -p 'typeof WebSocket'` → `undefined`; `node --experimental-websocket -p 'typeof WebSocket'` → `function`; `nix shell nixpkgs#nodejs_22 -c node -p 'typeof WebSocket'` → `function`. | `scripts/lib/cdp.mjs`, `packages/page-lab-pick.nix`, `README.md` |
| `F-CRI-UNAVAILABLE` | `chrome-remote-interface` is the canonical off-the-shelf raw-CDP client and is **declined**: adopting it means either an unpinned `npx -y` network fetch at runtime (the precedented `@latest`-skew failure this repo already carries with Kapture) or a nixpkgs attr that no longer exists (`nodePackages` removed). | 2026-09-06 | `nix eval nixpkgs#nodePackages.chrome-remote-interface` → attribute-missing error; `nix search nixpkgs chrome-remote-interface` → no result. | `packages/page-lab-pick.nix`, `references/pick-protocol.md` |
| `F-GATE-HOLE` | `checks.<system>.userscripts` globs `${self}/userscripts/*.user.js` — **this repo only**. nix-personal's `civitai-declutter` shipped with no `@license` and CI stayed green. The private tree must be linted by hand. | 2026-09-06 | `grep -n 'userscripts/\*' flake.nix` → the this-repo-only glob; then `bash scripts/userscript-meta-lint.sh <nix-personal>/userscripts/` and watch it report what CI never saw. | `README.md`, `.claude/skills/userscript-author/SKILL.md` |
| `F-GF-WEBHOOK-LENIENT` | Greasy Fork's webhook path uses `do_lenient_saving`, auto-confirming **every** warning including *version not incremented*. A push that changes code without bumping `@version` is **accepted** and silently never reaches installed users. | 2026-09-06 | Source: `gh search code --repo JasonBarnabe/greasyfork 'do_lenient_saving'`. Behaviour: push a code-only change through the sync webhook and confirm the script's version and install count do not move. | `skills/userscript-author/greasyfork.md` |
| `F-VM-GRANT-OMIT` | Omitting `@grant` is **not** `@grant none`. Omitted ⇒ sandboxed with `GM_info`/`GM.info`/`unsafeWindow` only (VM ≥ 2.32.0); `none` ⇒ sandbox disabled and **no `GM_*` at all**. | 2026-09-06 | Install two throwaway scripts on the same page, one omitting `@grant` and one with `@grant none`, each logging `typeof GM_info` and `typeof unsafeWindow`; compare the two console lines. | `skills/userscript-author/gm-api.md`, `skills/userscript-author/patterns.md` |

## UNVERIFIED — not facts yet

These four are **claims with recipes**, not measurements. Nothing may depend on one: each row names
the shape that ships until it is measured. Promote only by running the recipe and dating it.

| ID | Claim (UNVERIFIED) | Measured | How to re-measure | Cited by |
|---|---|---|---|---|
| `F-UNMEASURED-ESC` | **UNVERIFIED:** whether `Escape` cancels `searchForNode` inspect mode from a non-DevTools client (DevTools' own Esc handling lives in the frontend, which is absent here). Until measured, tier 1 promises **Ctrl-C only**. | — | Arm tier 1 (`page-lab-pick --timeout 45000`), press Escape in the browser, then click an element: if `Overlay.inspectNodeRequested` never fires and a normal click goes through, Escape cancels. **If it fails, leave the operator line as Ctrl-C only.** | `scripts/pick-element.mjs`, `references/routes.md`, `references/pick-protocol.md` |
| `F-UNMEASURED-KAPTURE-AWAIT` | **UNVERIFIED:** that Kapture's `evaluate` (`awaitPromise:true`) blocks on a returned Promise for the full timeout. Tier 3's one-call shape depends on it; the ARM/POLL/DISARM shape is the documented fallback. | — | With the eval toggle on, `mcp__kapture__evaluate({tabId, code:"new Promise(r=>setTimeout(()=>r('late'),10000))", timeout:20000})` and time the call: ≥10 s and `'late'` means it blocks. **If it fails, tier 3 becomes ARM/POLL/DISARM, same shape as tier 4.** | `references/routes.md`, `references/pick-protocol.md`, `scripts/pick-payload.js` |
| `F-UNMEASURED-KAPTURE-HOVER` | **UNVERIFIED:** that `elements({selector:':hover'})` returns the **whole** hover stack. Kapture's tool description says *"Query all elements matching a CSS selector"* while its `selector` param says *"uses first matching element"*. Tier 2's stack-sampling depends on the former. | — | Connect a tab, hover a deeply nested element, then `mcp__kapture__elements({tabId, selector:"%3Ahover"})` and count entries: one means first-match-only. **If it fails, tier 2b demotes to `elementsFromPoint` with operator-supplied coordinates and `routes.md` says so.** | `references/routes.md`, `references/pick-protocol.md` |
| `F-UNMEASURED-DISCONNECT-RESET` | **UNVERIFIED:** whether Chrome auto-resets inspect mode on CDP client disconnect. Do **not** write "the socket close cleans it up" anywhere. | — | Arm one page target with `h_cdp Overlay.setInspectMode '{"mode":"searchForNode","highlightConfig":{...}}'`, let the socket close **without** disarming, then click in that tab: a swallowed click means no auto-reset. Record either way. | `references/pick-protocol.md`, `scripts/pick-watchdog.mjs` |
